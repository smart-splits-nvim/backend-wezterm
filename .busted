-- Busted test configuration
return {
  _all = {
    lua = 'nlua',
    helper = 'tests/init.lua',
    pattern = '_spec',
  },
  default = { ROOT = { 'tests/core', 'tests/plugin' } },
  core = { ROOT = { 'tests/core' } },
  plugin = { ROOT = { 'tests/plugin' } },
}
